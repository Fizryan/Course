package main

import "fmt"

func main() {

	type Tanggal struct {
		title         string
		pages, prices int
	}

	var beli, jual, pasar Tanggal
	fmt.Scan(&pasar.title, &pasar.pages, &pasat.prices)

}
